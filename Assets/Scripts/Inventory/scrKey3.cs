using System;
using UnityEngine;

public class scrKey3 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
       Debug.Log("hey key 3 got instantiated"); 
    }

    // display a key icon on top of player.
    private void Update()
    {
        
    }
}
