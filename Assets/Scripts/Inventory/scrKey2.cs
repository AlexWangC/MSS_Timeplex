using UnityEngine;

public class scrKey2 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
       Debug.Log("hey key 2 got instantiated"); 
    }
}
